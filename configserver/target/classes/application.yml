spring:
    application:
        name: config-server
    profiles:
        # active: native
         active: git
    cloud:
        config:
            server:
                native:
                    # search-locations: classpath:/config
                     search-locations: file:///c:\temp\config
                git:
                    uri: https://github.com/HyunHyun-choi/test.git
server:
    port: 8071
